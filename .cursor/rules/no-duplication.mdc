---
description: Réutilisation obligatoire — jamais de double composant, jamais de code dupliqué, jamais de code mort. Advisory (jscpd/knip disponibles en commande, hooks non câblés).
alwaysApply: true
---

# Pas de double, pas de code mort (règle plancher — advisory)

Avant de créer **tout** composant / fonction / hook / module :

1. **Cherche `docs/COMPONENT_INDEX.md`** (index généré de tous les composants) + grep le nom.
   S'il existe déjà → **réutilise-le**. Ne crée pas un 2ᵉ exemplaire.
2. Il existe mais est en mauvais état → **refactore-le EN PLACE** et garde le même fichier/export.
   Ne forke pas `Foo` en `Foo2` / `FooNew` / `FooV2`.
3. Tu remplaces un composant → **supprime l'ancien dans le même change**. Pas d'orphelin.

## Statut réel : advisory — outils dispo, hooks NON câblés

> Réalité vérifiée : `.husky/pre-commit` ne lance QUE `secrets-scan.sh` (jscpd retiré
> le 2026-06-26 — « front + DS fully unbraked »). `.husky/pre-push` ne câble PAS knip
> (retiré le 2026-06-18). Donc rien ne **bloque** automatiquement la duplication / le
> code mort — la discipline repose sur l'étape 1 (chercher avant de créer) + les
> commandes ci-dessous, lancées à la main.

- **Duplication** : `node scripts/quality-gate.mjs dup` (jscpd, baseline **143 clones**) —
  à lancer soi-même. Non câblé en pre-commit.
- **Code mort** : `node scripts/quality-gate.mjs dead` (knip, baseline **22 findings**) —
  à lancer soi-même. Non câblé en pre-push.
- Le baseline sert de repère de non-régression ; il n'y a **pas** de blocage automatique.
- Pour re-transformer ces outils en gates bloquants : ré-ajouter leur ligne dans le hook
  husky concerné (décision séparée — ne pas le faire au détour d'une autre passe).

## Inspecter / réparer

- `pnpm quality` — lance les deux vérifications (advisory).
- `pnpm quality:dup:report` — rapport HTML des clones (où dupliques-tu ?).
- `pnpm quality:dead:report` — liste knip du code mort (quel orphelin supprimer ?).
- `pnpm quality:update` — abaisse le baseline après un vrai nettoyage (jamais pour le relâcher).

## Limite

jscpd attrape la duplication **textuelle/structurelle** (copier-coller, fork), pas la duplication
**sémantique** (même logique réécrite autrement). D'où l'étape 1 obligatoire : consulter l'index
AVANT de coder. En cas de doute « est-ce que ça existe déjà ? » → cherche, ne recode pas.

Exceptions légitimes (ne pas « réparer ») : les composants marqués 🔌 *intentionally unwired* dans
l'index (construits, testés, à brancher plus tard) — ils sont allowlistés, ne les supprime pas.
