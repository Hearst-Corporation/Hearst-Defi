# Dependencies
node_modules
.pnpm-store
package-lock.json

# Next.js
.next
.next.trash/
out

# Production
build
dist

# Misc
.DS_Store
*.pem
.vscode
.idea

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env*.local

# Typescript
*.tsbuildinfo
next-env.d.ts

# Database
prisma/dev.db
prisma/dev.db-journal
prisma/**/*.db
prisma/**/*.db-journal

# Logs
logs
*.log

# Test coverage
coverage/

# Playwright artifacts
playwright-report/
test-results/
.tmp/

# Generated audit reports (HTML reports + screenshots only — durable .md packs tracked below)
docs/audit/*.html
docs/audit/screenshots/
docs/audit/*.md

# Ephemeral agent worktrees
.claude/worktrees/
.worktrees/

# Scratch / audit artifacts (screenshots, dumps) — never committed
tmp/

# Playwright MCP screenshots / snapshots
.playwright-mcp/
.density-validation-screenshots/

# Out-of-repo scratch projects dropped in the root (Figma export / docs vault) —
# separate apps, never part of the connect build/typecheck.
figma-investor-deck/
hearst-docs-vault/

advanced-metrics-fixed.png
.vercel

# Electron build artifacts
dist-electron/
release/
out/
electron/__tests__/screenshots/
electron/__tests__/results/
*.dmg
*.exe
*.AppImage
*.deb
*.zip

# Tri / corbeille locale (videe manuellement)
.tri-trash/

# Corbeille / archive locale du clean-workspace (jamais versionnee) — voir scripts/clean-workspace.sh
.scratch/

# Captures de dev a la racine (non versionnees)
/*.png
/*.jpeg
/*.jpg


# Local release artifacts (CREDENTIALS contains a password — never commit)
releases/
uiux-audit-*.html
tri-audit-*.html

# Dev artifacts (root)
/dev.db
/audit_profile.py
/portfolio_body.html

# jscpd duplication report (audit artifact)
.jscpd-report/

# Env backups (may contain secrets — never commit)
.env*
*.env.bak*
.env.local.bak*

# design-sync (claude.ai/design) — staged scripts + regenerated build output
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/compiled-foundation.css
.design-sync/ds-entry.tsx

# Telegram machine-price generated artifact (regenerated on demand)
telegram-machines.html
.sweep/

# Storybook build output — regenerated on demand, never committed
storybook-static/

# Licensed Tailwind Plus kits — local reference only, committing/pushing them
# to GitHub would redistribute licensed content. Source: ~/Downloads.
design-refs/
.storybook-consulted
