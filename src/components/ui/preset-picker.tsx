/**
 * Compat façade — single-DS convergence.
 * Canonical source moved to `@/components/catalyst/preset-picker`.
 * This thin re-export preserves the legacy `@/components/ui/preset-picker` path
 * and its `PresetPicker` / `PresetPickerOption` API byte-for-byte. Do not add
 * logic here.
 */
export { PresetPicker } from "@/components/catalyst/preset-picker";
export type { PresetPickerOption } from "@/components/catalyst/preset-picker";
