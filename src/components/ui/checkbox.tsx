/**
 * Compat façade — single-DS convergence.
 * Canonical source moved to `@/components/catalyst/choice-checkbox`.
 * This thin re-export preserves the legacy `@/components/ui/checkbox` path
 * and its `Checkbox` API byte-for-byte. Do not add logic here.
 */
export { Checkbox } from "@/components/catalyst/choice-checkbox";
