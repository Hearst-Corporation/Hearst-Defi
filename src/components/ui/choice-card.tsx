/**
 * Compat façade — single-DS convergence.
 * Canonical source moved to `@/components/catalyst/choice-card`.
 * This thin re-export preserves the legacy `@/components/ui/choice-card` path
 * and its `ChoiceCard` / `ChoiceGroup` API byte-for-byte. Do not add logic here.
 */
export { ChoiceCard, ChoiceGroup } from "@/components/catalyst/choice-card";
