/**
 * Compatibility re-export — canonical Card primitives live in
 * `@/components/catalyst/card`. New code should import from catalyst directly.
 */
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from "@/components/catalyst/card";
