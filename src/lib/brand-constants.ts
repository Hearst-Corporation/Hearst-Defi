/** Literal hex for JS SDKs (Privy, etc.) — CSS source of truth: `--ct-accent` in cockpit.css */
export const CONNECT_ACCENT_HEX = "#9E1B2E" as const;

/** Semantic success/live green — decoupled from brand accent (cockpit.css `--ct-status-success`). */
export const CONNECT_SUCCESS_HEX = "#16A34A" as const;
