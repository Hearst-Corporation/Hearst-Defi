import "server-only";

import { isPrivyConfigured } from "@/lib/auth/is-privy-configured";
import { isSumsubConfigured, sumsubLevelName } from "@/lib/onboarding/sumsub";

export { isPrivyConfigured, isSumsubConfigured, sumsubLevelName };
